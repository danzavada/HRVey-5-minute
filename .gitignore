# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~

# Node (none committed, but just in case)
node_modules/
npm-debug.log*

# Local scratch
*.tmp

# real ECGs — never commit (only the synthetic EXAMPLE is tracked)
*.pdf
!EXAMPLE/synthetic_ecg_5min.pdf
broken ECGs/
